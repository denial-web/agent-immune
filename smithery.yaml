startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
  commandFunction: |-
    (config) => ({
      command: 'python',
      args: ['-m', 'agent_immune', 'serve', '--transport', 'stdio']
    })
  exampleConfig: {}
